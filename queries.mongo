{ "job_details.programming_languages": {"$elemMatch": {"$regex": ".*python.*", "$options": "i"}}}
{ "job_post.title": {"$regex": ".*java.*","$options": "i"}}
{ "$or" : [{"job_details.programming_languages": {"$elemMatch": {"$regex": ".*python.*", "$options": "i"}}}, {"job_details.technologies": {"$elemMatch": {"$regex": ".*python.*", "$options": "i"}}}]}
